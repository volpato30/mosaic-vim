## this is the vim settings.

~~~~
cd ~
git clone https://github.com/volpato30/mosaic-vim.git ~/.vim
ln -s ~/.vim/vimrc ~/.vimrc
ln -s ~/.vim/gvimrc ~/.gvimrc
cd ~/.vim
git submodule init
git submodule update
~~~~
